// main.jsx — Vite entry. Styles first, then the app (which mounts itself
// via ReactDOM.createRoot at the bottom of app.jsx), then the drag helper.
import "./styles.css";
import "./app.jsx";
import "./drag-arrange.js";
