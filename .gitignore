# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Dependencies / build output (if you later add a build step)
node_modules/
dist/
build/

# Bundled single-file export (regenerate as needed)
*-standalone.html

# local env (keys)
.env
.env.local

.playwright-mcp/

# Playwright e2e artifacts
test-results/
playwright-report/
playwright-report.json
/blob-report/
/.cache/

# local machine prefs (Claude finish-sound phrase)
.claude/finish-phrase
